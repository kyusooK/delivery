apiVersion: v1
kind: Service
metadata:
  name: notification-management
  labels:
    app: notification-management
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: notification-management